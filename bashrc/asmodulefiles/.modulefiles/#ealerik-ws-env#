#%Module1.0


setenv LESS -MXR
setenv EDITOR emacsclient

set-alias cgrep "grep --color=always"
set-alias cdl "cd `ls -td * | head -1`"
set-alias cp "cp -i"
set-alias mv "mv -i"
set-alias rm "rm -i"

set-alias ls "ls -F --color=auto"
set-alias l less
set-alias h history
set-alias git-env "module add ealerik-git-env"
set-alias lwget "wget --no-proxy"

# A nice ps
set-alias pss "/usr/ucb/ps -auxwww"


#setenv LOCAL_WORKSPACE /local/$env(LOGNAME)/workspace

%
#module add make
#prepend-path PATH $env(ECLIPSE_WORKSPACE)/uctool/build

#prepend-path PATH /scripts

prepend-path PATH . 

# ccache
#setenv CCACHE_NLEVELS 3
#setenv CCACHE_NOLINK true
setenv CCACHE_UMASK 002

#shortcuts
#set-alias valgrind "valgrind --leak-check=full"

#set-alias terminator "nohup /proj/ltesim/usr/ealerik/bin/terminator-0.96/terminator"
#set-alias terminator "terminator -x bash"
set-alias oo "soffice"

